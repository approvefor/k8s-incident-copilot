# Configure an S3 backend for team usage. Local state is enough for the demo.
# terraform {
#   backend "s3" {
#     bucket = "ai-platform-terraform-state"
#     key    = "dev/terraform.tfstate"
#     region = "eu-central-1"
#   }
# }
